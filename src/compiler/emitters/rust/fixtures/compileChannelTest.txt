use super::super::wirespec::*;
use regex;
pub mod Queue {
    use super::*;
    pub trait Sender {
        fn queue(message: &str);
    }
    pub trait Listener {
        fn queue(handler: Box<dyn Fn(String) -> ()>);
    }
}

#![allow(warnings)]
pub mod model;
pub mod endpoint;
pub mod wirespec;
