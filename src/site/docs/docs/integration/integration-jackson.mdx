---
title: Jackson
slug: /integration/jackson
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

In this document ...

Wirespec can sometimes conflict with the language specifics of the target language.
For this scenario we have made the jackson integration

## Setup

<Tabs>
    <TabItem value="maven" label="Maven">
        ```xml title="pom.xml"
        <dependency>
            <groupId>community.flock.wirespec.integration</groupId>
            <artifactId>jackson</artifactId>
            <version>0.14.3</version>
        </dependency>
        ```
    </TabItem>
    <TabItem value="gradle" label="Gradle">
        ```gradle title=""
        implementation("community.flock.wirespec.integration:jackson:0.14.3")
        ```
    </TabItem>
</Tabs>

:::info
Make sure to use the latest version, found here:

![Maven Central](https://img.shields.io/maven-central/v/community.flock.wirespec.plugin.maven/wirespec-maven-plugin)
:::

------------------------------

## Serialization example

Example

Serializer/Deserializer for

Enum
Java Reserved keywords
Kotlin reserved keywords
Refined


```
    addSerializer(Wirespec.Refined::class.java, RefinedSerializer())
    addSerializer(Wirespec.Enum::class.java, EnumSerializer())
    setDeserializerModifier(WirespecDeserializerModifier())
    setNamingStrategy(KotlinReservedKeywordNamingStrategy())
```

